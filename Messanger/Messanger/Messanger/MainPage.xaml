<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Messanger"
             x:Class="Messanger.MainPage">

    <ContentPage.BindingContext>
        <local:ViewModelVk/>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout>
            <Entry x:Name="LoginEntry" Text="{Binding Login}"/>
            <Entry x:Name="PasswordEntry" Text="{Binding Password}"/>
            <Label Text="{Binding AccauntName}"/>
            <Button Command="{Binding auth}"/>
        </StackLayout>
    </ContentPage.Content>
   
    
  
</ContentPage>
